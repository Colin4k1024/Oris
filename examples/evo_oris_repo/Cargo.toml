[package]
name = "evo_oris_repo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
oris-runtime = { path = "../../crates/oris-runtime", features = ["full-evolution-experimental"] }
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
