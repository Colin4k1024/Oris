# Longline Travel Experience: Beijing -> Shanghai

This baseline document is intentionally tracked so references to
`docs/evolution/travel-beijing-shanghai-experience.md` are always concrete.

The runtime demo writes a run-scoped version of this file under:

- `docs/evokernel/demo_runs/<run_id>/producer-workspace/docs/evolution/travel-beijing-shanghai-experience.md`

That run-scoped file is the actual artifact used when producing mutation diffs
and capturing reusable experience assets.
